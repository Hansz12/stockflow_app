name: stockflow_app
description: "StockFlow Inventory App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  # Database
  supabase_flutter: ^2.8.3
  # Scanner
  mobile_scanner: ^5.2.3 
  # Styling & Formatting (NEW)
  google_fonts: ^6.1.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true